package com.sda.datingapp.model;

public class ProfileAttributeType {
    public enum Drinks {
        NEVER, SOCIALLY, OFTEN
    }

    public enum Drugs {
        NEVER, SOCIALLY, OFTEN
    }
    public enum Education {
        MASTER, BACHELOR, HIGH_SCHOOL, PHD, OTHER
    }
}
